import { enableProdMode } from '@angular/core';

const environment = {
  production: true,
  apiUrl: 'https://yourusername.github.io/cahat-bot'
};

enableProdMode();

export const environment = {
  production: true
};
